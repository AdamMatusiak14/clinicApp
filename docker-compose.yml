services:
  backend:
    build:
      context: ./clinic
      dockerfile: Dockerfile
    container_name: clinic-backend
    ports:
      - "8080:8080"
    environment:
      JWT_SECRET: "mojBardzoDlugiSekretJWT2026Min32Znaki"
    restart: unless-stopped

  frontend:
    build:
      context: ./frontend/my-app
      dockerfile: Dockerfile
    container_name: clinic-frontend
    depends_on:
      - backend
    ports:
      - "3000:80"
    environment:
      API_URL: http://backend:8080
    restart: unless-stopped
